name: Deploy to Azure

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy-backend:
    name: Deploy Backend to Azure
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      
      - name: Install dependencies
        working-directory: ./LU_Backend
        run: npm ci
      
      - name: Build application
        working-directory: ./LU_Backend
        run: npm run build

      - name: Package backend dist
        working-directory: ./LU_Backend
        run: |
          mkdir -p deploy
          cp -r dist deploy/dist
          cp package.json package-lock.json deploy/
          (cd deploy && zip -r ../backend.zip .)
      
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'lu-backend-linux'
          publish-profile: ${{ secrets.AZURE_BACKEND_PUBLISH_PROFILE }}
          package: ./LU_Backend/backend.zip

  build-and-deploy-frontend:
    name: Deploy Frontend to Azure
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      
      - name: Install dependencies
        working-directory: ./LU_Frontend/lu_frontend
        run: npm ci
      
      - name: Build application
        working-directory: ./LU_Frontend/lu_frontend
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
      
      - name: Package Next.js standalone
        working-directory: ./LU_Frontend/lu_frontend
        run: |
          rm -rf deploy
          mkdir -p deploy/.next/static
          cp -r .next/standalone/* deploy/
          cp -r .next/static deploy/.next/static
          cp -r public deploy/public || true
          cp package.json package-lock.json deploy/
      
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'lu-frontend-linux'
          publish-profile: ${{ secrets.AZURE_FRONTEND_PUBLISH_PROFILE }}
          package: ./LU_Frontend/lu_frontend/deploy